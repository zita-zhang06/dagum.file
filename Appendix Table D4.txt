from pathlib import Path

import numpy as np
import pandas as pd
from openpyxl.styles import Alignment, Font, PatternFill
from scipy.stats import burr


def find_data_file():
    names = ("EMDAT(2)(1).xlsx.xlsx", "EMDAT(2)(1).xlsx", "EMDAT.xlsx")
    for folder in (Path.cwd(), Path.home() / "Desktop"):
        for name in names:
            path = folder / name
            if path.exists():
                return path
    raise FileNotFoundError("Put EMDAT(2)(1).xlsx.xlsx on the Desktop or in the current folder.")


DATA_FILE = find_data_file()
OUTPUT_FILE = DATA_FILE.parent / "Table_D4_Full_Theta_Grid.xlsx"
DAMAGE_COLUMN = "Total Damage ('000 US$)"
GDP_COLUMN = "GDP (current US$)"
FLOOD_SUBTYPES = ("Flash flood", "Flood (General)", "Riverine flood")
PARETO_THRESHOLD = 2e-5

events = pd.read_excel(DATA_FILE, sheet_name="Data all")
gdp = pd.read_excel(DATA_FILE, sheet_name="GDP")
events.columns = events.columns.astype(str).str.strip()
gdp.columns = gdp.columns.astype(str).str.strip()
events["Start Year"] = pd.to_numeric(events["Start Year"], errors="coerce")
events[DAMAGE_COLUMN] = pd.to_numeric(events[DAMAGE_COLUMN], errors="coerce")
gdp["Year"] = pd.to_numeric(gdp["Year"], errors="coerce")
gdp[GDP_COLUMN] = pd.to_numeric(gdp[GDP_COLUMN], errors="coerce")
mask = (
    events["Country"].astype(str).str.strip().eq("Brazil")
    & events["Disaster Type"].astype(str).str.strip().eq("Flood")
    & events["Disaster Subtype"].astype(str).str.strip().isin(FLOOD_SUBTYPES)
    & events["Start Year"].between(2000, 2024)
    & events[DAMAGE_COLUMN].notna()
    & events[DAMAGE_COLUMN].gt(0)
)
events = events.loc[mask].copy()
events["Year"] = events["Start Year"].astype(int)
gdp = gdp.loc[gdp["Year"].between(2000, 2024), ["Year", GDP_COLUMN]].copy()
gdp["Year"] = gdp["Year"].astype(int)
data = events.merge(gdp, on="Year", how="left", validate="many_to_one")
if data[GDP_COLUMN].isna().any():
    raise ValueError("GDP is missing for one or more event years.")
data["damage_ratio"] = data[DAMAGE_COLUMN] * 1000.0 / data[GDP_COLUMN]
x_dagum = np.sort(data.loc[data["damage_ratio"] > 0, "damage_ratio"].to_numpy(float))
x_pareto = np.sort(x_dagum[x_dagum >= PARETO_THRESHOLD])
n_dagum, n_pareto = len(x_dagum), len(x_pareto)
if (n_dagum, n_pareto) != (51, 38):
    raise ValueError(f"Expected 51 Dagum and 38 Pareto observations; got {n_dagum} and {n_pareto}.")

lambda_dagum = n_dagum / 25
lambda_pareto = n_pareto / 25
pareto_alpha = n_pareto / np.sum(np.log(x_pareto / PARETO_THRESHOLD))
dagum_a, dagum_p, _, dagum_b = burr.fit(x_dagum, floc=0)

dt = 1 / 12
n_steps = 600
n_simulations = 5000
theta_grid = np.round(np.arange(0.1, 3.01, 0.1), 2)
last_window_steps = 60
last_window_start = n_steps - last_window_steps


def generate_jumps(model, arrival_rate, seed):
    rng = np.random.default_rng(seed)
    counts = rng.poisson(arrival_rate * dt, size=(n_simulations, n_steps)).astype(np.int16)
    flat = counts.ravel()
    indices = np.repeat(np.arange(flat.size, dtype=np.int32), flat)
    u = np.clip(rng.random(int(flat.sum())), 1e-12, 1 - 1e-12)
    if model == "Pareto":
        losses = PARETO_THRESHOLD * (1 - u) ** (-1 / pareto_alpha)
    else:
        losses = dagum_b * (u ** (-1 / dagum_p) - 1) ** (-1 / dagum_a)
    return np.bincount(indices, weights=losses, minlength=flat.size).reshape(
        n_simulations, n_steps
    )


jumps_p = generate_jumps("Pareto", lambda_pareto, 24680)
jumps_d = generate_jumps("Dagum", lambda_dagum, 13579)
recovery_factors = (1 - theta_grid * dt)[:, None]
current_p = np.zeros((len(theta_grid), n_simulations))
current_d = np.zeros((len(theta_grid), n_simulations))
last_sum_p = np.zeros_like(current_p)
last_sum_d = np.zeros_like(current_d)

for step in range(n_steps):
    current_p = np.clip(current_p * recovery_factors + jumps_p[:, step][None, :], 0, 1)
    current_d = np.clip(current_d * recovery_factors + jumps_d[:, step][None, :], 0, 1)
    if step >= last_window_start:
        last_sum_p += current_p
        last_sum_d += current_d

table = pd.DataFrame(
    {
        "θ": theta_grid,
        "Pareto probability (%)": np.mean(last_sum_p / 60 >= 0.01, axis=1) * 100,
        "Dagum probability (%)": np.mean(last_sum_d / 60 >= 0.01, axis=1) * 100,
    }
)

with pd.ExcelWriter(OUTPUT_FILE, engine="openpyxl") as writer:
    table.to_excel(writer, sheet_name="Table D4", index=False)
    ws = writer.book["Table D4"]
    for cell in ws[1]:
        cell.font = Font(bold=True)
        cell.fill = PatternFill("solid", fgColor="D9D9D9")
        cell.alignment = Alignment(horizontal="center")
    ws.column_dimensions["A"].width = 12
    ws.column_dimensions["B"].width = 25
    ws.column_dimensions["C"].width = 25
    for row in ws.iter_rows(min_row=2):
        for cell in row:
            cell.alignment = Alignment(horizontal="center")
            cell.number_format = "0.00"

print(table.to_string(index=False, formatters={"θ": "{:.2f}".format, "Pareto probability (%)": "{:.2f}".format, "Dagum probability (%)": "{:.2f}".format}))
print("Saved:", OUTPUT_FILE)